// Payment inputs are now handled by Stripe's <PaymentElement> inside
// StripeCheckoutForm. This component is kept as a section placeholder only.
export default function PaymentMethod() {
  return null;
}
